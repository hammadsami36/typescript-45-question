var userName = ["admin", "ayesha", "maryam", "amber", "alishba"];
if (userName.length > 0) {
    console.log("we have enough users");
}
else {
    userName = [];
    console.log("we need to some find users!");
}
