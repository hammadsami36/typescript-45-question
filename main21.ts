interface item {
    name : string
    price : number
}
// two object
const vegetables: item = {
name: `onion`,
price: 100
}

const fruit: item = {
    name: `Apple`,
    price: 200
    }
    console.log(`vegetables name: ${vegetables.name}, vegetables price: ${vegetables.price}`)
    console.log(`fruit name: ${fruit.name}, fruit price: ${fruit.price}`)
